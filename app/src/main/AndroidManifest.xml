<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".navigationBar"
            android:exported="true"
            android:label="@string/title_activity_navigation_bar" />
        <activity
            android:name=".landingPage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".HomePage"></activity>
        <activity android:name=".Account"></activity>
        <activity android:name=".orderHistory"></activity>
        <activity android:name=".cart"></activity>
        <activity android:name=".changePassword"></activity>
        <activity android:name=".rice_page"></activity>
        <activity android:name=".Reservation_home"></activity>


    </application>

</manifest>